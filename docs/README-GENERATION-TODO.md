# README Generation TODO

Placeholder for README generation tooling (BUILD-README-VALIDATION-001).

Planned steps:
1. Extract XML docs from public APIs and build package-level README sections.
2. Compare generated README with existing to enforce freshness.
3. Integrate with CI via dotnet tool or script.

Pending Implementation:
- Tool: /tools/DocGen (future project).
- Command: `dotnet docgen --check`.

This file marks the remaining Phase 1 deferred item now that Phase 2+ features are in place.
